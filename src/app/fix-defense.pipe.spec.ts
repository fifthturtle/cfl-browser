import { FixDefensePipe } from './fix-defense.pipe';

describe('FixDefensePipe', () => {
  it('create an instance', () => {
    const pipe = new FixDefensePipe();
    expect(pipe).toBeTruthy();
  });
});
